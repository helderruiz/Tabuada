function tabuada() {
    let numero = document.getElementById('numero')
    let tabuadaArea = document.getElementById('tabuada-area')
    if (numero.value.length == 0) {
        alert('Digite um número!')
    } else
    let numeroValor = Number(numero.value)
}

